version: 2

project_name: jr-stack

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/jr-stack
    binary: jr-stack
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

brews:
  - repository:
      owner: JuanCruzRobledo
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    name: jr-stack
    homepage: "https://github.com/JuanCruzRobledo/jr-stack"
    description: "JR Stack — OPSX Edition. One command to configure any AI coding agent on any OS."
    license: "MIT"
    commit_msg_template: "chore: update jr-stack formula to {{ .Tag }}"

scoops:
  - repository:
      owner: JuanCruzRobledo
      name: scoop-bucket
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    name: jr-stack
    homepage: "https://github.com/JuanCruzRobledo/jr-stack"
    description: "JR Stack — OPSX Edition. One command to configure any AI coding agent on any OS."
    license: MIT
    commit_msg_template: "chore: update jr-stack to {{ .Tag }}"
